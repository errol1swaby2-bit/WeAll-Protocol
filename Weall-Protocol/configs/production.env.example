# WeAll production runtime example
# Copy to your managed environment or secret-injection layer.
# Do not commit real secret values.

WEALL_MODE=prod
WEALL_CHAIN_MANIFEST_PATH=./configs/chains/weall-genesis.json
WEALL_REQUIRE_CHAIN_MANIFEST=1
WEALL_CHAIN_ID=weall-prod
WEALL_TX_INDEX_PATH=./generated/tx_index.json
WEALL_CORS_ORIGINS=https://weallprotocol.xyz,https://app.weallprotocol.xyz

# Identity / validator
WEALL_NODE_ID=@validator-1
WEALL_VALIDATOR_ACCOUNT=@validator-1
WEALL_NODE_PRIVKEY_FILE=/run/secrets/weall_node_privkey
WEALL_NODE_PUBKEY_FILE=/run/secrets/weall_node_pubkey
WEALL_VALIDATOR_ACCOUNT_FILE=/run/secrets/weall_validator_account

# Networking / validator posture
WEALL_NET_ENABLED=1
WEALL_BFT_ENABLED=1
WEALL_VALIDATOR_SIGNING_ENABLED=1
WEALL_SIGVERIFY=1
WEALL_STRICT_TX_SIG_DOMAIN=1
WEALL_SYNC_REQUIRE_TRUSTED_ANCHOR=1
WEALL_STATE_SYNC_REQUIRE_TRUSTED_ANCHOR=1

# PoH email verification is delegated to registered WeAll-hosted oracle services.
# credentials or PoH email challenge secrets.

# Gunicorn
GUNICORN_BIND=0.0.0.0:8000
GUNICORN_WORKERS=1
GUNICORN_TIMEOUT=120
GUNICORN_LOG_LEVEL=info
