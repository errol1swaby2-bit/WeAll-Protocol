name: Claim Surface Registry Hardening Once

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  harden:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Paused while the resume helper is staged."
