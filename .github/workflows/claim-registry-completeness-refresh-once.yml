name: Claim Registry Completeness Refresh Once

on:
  push:
    branches:
      - claim-registry-completeness-hardening
    paths:
      - .github/workflows/claim-registry-completeness-refresh-once.yml

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: claim-registry-completeness-hardening

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install locked backend dependencies
        working-directory: Weall-Protocol
        run: |
          python -m pip install --upgrade pip
          pip install --require-hashes -r requirements-dev.lock
          pip install -e . --no-deps

      - name: Delete helper, format, regenerate, commit, validate exact commit, and push
        shell: bash
        run: |
          set -Eeuo pipefail
          rm .github/workflows/claim-registry-completeness-refresh-once.yml
          cd Weall-Protocol
          ruff format scripts/check_public_claim_freshness.py tests/test_current_document_registry.py
          ruff check scripts/check_public_claim_freshness.py tests/test_current_document_registry.py
          PYTHONPATH=src python scripts/compile_v2_spec.py
          cd ..
          git add -A
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Enforce current claim-surface classification coverage'
          FINAL_SHA="$(git rev-parse HEAD)"
          cd Weall-Protocol
          python scripts/check_generated.py
          PYTHONPATH=src python scripts/compile_v2_spec.py --check
          python scripts/check_v2_spec_clean_checkout.py
          PYTHONDONTWRITEBYTECODE=1 python scripts/check_v15_public_readiness_artifacts.py
          python scripts/check_public_claim_freshness.py
          python scripts/gen_current_verified_claims.py --check
          pytest -q tests/test_current_document_registry.py tests/test_release_docs_truth_sync.py tests/test_reviewer_language_cleanup.py
          cd ..
          test "$(git rev-parse HEAD)" = "$FINAL_SHA"
          test -z "$(git status --porcelain)"
          git push origin HEAD:claim-registry-completeness-hardening
