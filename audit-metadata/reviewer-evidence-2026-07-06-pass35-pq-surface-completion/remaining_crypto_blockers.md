# Remaining crypto blockers

- Browser-local ML-DSA signing is still not complete; controlled/public testnet user signing must use backend/operator-controlled ML-DSA signing until browser support is implemented and reviewed.
- Durable PQ signing policy for public evidence bundles still needs standardization before long-lived public proof packages.
- External cryptographic review remains required before any long-lived public network or mainnet claim.
