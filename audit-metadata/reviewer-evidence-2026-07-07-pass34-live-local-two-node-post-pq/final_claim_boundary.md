The live local two-node rehearsal exercises post-PQ controlled-devnet paths in a local environment. It supports reviewer confidence that the pq-mldsa-v1 transition did not break live local two-node convergence, signed transaction submission, state sync, or chain identity parity, but it does not replace fresh external two-machine observer/operator evidence or external cryptographic review.
